"""Промпт для буквальной расшифровки рукописного текста с фото (OCR) в формате JSON."""

OCR_PROMPT = """Ты распознаёшь текст школьного диктанта с фотографии построчно.

Правила:
1. Распознавай текст строго построчно — каждая строка на изображении становится одним элементом в массиве lines.
2. НЕ исправляй орфографические и пунктуационные ошибки ученика.
3. НЕ угадывай слова по смыслу — только то, что реально видно.
4. Сохраняй переносы строк: одна строка рукописи = один элемент в lines.
5. Если фрагмент или слово неразборчивы — подставь ровно: [[неразборчиво]].
6. В uncertain_spans укажи номера строк (индекс с 0) и фрагменты, в которых ты не уверен (или где стоит [[неразборчиво]]).
7. Оцени общую уверенность распознавания: high (почти всё ясно), medium (есть сомнения), low (много неразборчивого или плохое качество).

Ответь строго в формате JSON, без markdown и без текста до/после:
{
  "lines": ["первая строка текста", "вторая строка", ...],
  "uncertain_spans": [{"line_index": 0, "fragment": "слово или [[неразборчиво]]"}, ...],
  "ocr_confidence": "high"
}

Значение ocr_confidence — ровно одно из: "high", "medium", "low".
Массив lines не пустой. Если строк на фото нет или изображение не текст — верни lines: [] и ocr_confidence: "low"."""
